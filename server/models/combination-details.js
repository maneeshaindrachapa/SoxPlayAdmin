'use strict';
var request = require('request');
var at = require('../../server/config.json').facebook_api_token;

// expire code is(error.code ==) 190
module.exports = function(CombinationDetails) {
};
